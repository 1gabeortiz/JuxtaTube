import { PlaceholderPanel } from '../components/ui/PlaceholderPanel';

export function CompetitorsPage() {
  return (
    <PlaceholderPanel
      title="Competitors"
      description="Current stats and growth-over-time charts for hand-picked channels in the beat-making space. The YouTube API has no history for channels you don't own, so this app builds that history itself with a daily snapshot job."
    />
  );
}
