import { PlaceholderPanel } from '../components/ui/PlaceholderPanel';

export function ContentToolsPage() {
  return (
    <PlaceholderPanel
      title="Content Tools"
      description="Official YouTube tag suggestions for your own videos, plus a tag explorer that ranks which tags are actually being used across a channel or keyword in your niche."
    />
  );
}
