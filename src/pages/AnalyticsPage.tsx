import { PlaceholderPanel } from '../components/ui/PlaceholderPanel';

export function AnalyticsPage() {
  return (
    <PlaceholderPanel
      title="Analytics"
      description="Watch time, audience demographics, and traffic sources. This data is private to the channel owner, so it requires connecting the channel over OAuth."
    />
  );
}
